cmake_minimum_required(VERSION 3.20)
project(UltraPaste VERSION 0.1.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

if(MINGW)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -static-libgcc -static-libstdc++ -w")
endif()

add_library(UltraPaste SHARED
    src/plugin.cpp
    src/reaper_media_parser.cpp
)

target_include_directories(UltraPaste PRIVATE
    ${CMAKE_SOURCE_DIR}/aviutl2_sdk
    ${CMAKE_SOURCE_DIR}/src
)

set_target_properties(UltraPaste PROPERTIES
    PREFIX ""
    SUFFIX ".aux2"
)
